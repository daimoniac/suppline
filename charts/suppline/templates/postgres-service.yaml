{{- if .Values.postgres.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "suppline.fullname" . }}-postgres
  labels:
    {{- include "suppline.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgres
spec:
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP
  selector:
    {{- include "suppline.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: postgres
{{- end }}
