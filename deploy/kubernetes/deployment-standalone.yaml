# Standalone deployment without Trivy sidecar
# Use this with trivy-deployment.yaml for separate Trivy server
apiVersion: apps/v1
kind: Deployment
metadata:
  name: daimoniac/suppline
  namespace: daimoniac/suppline
  labels:
    app: daimoniac/suppline
spec:
  replicas: 1
  strategy:
    type: Recreate  # Use Recreate to avoid multiple instances with SQLite
  selector:
    matchLabels:
      app: daimoniac/suppline
  template:
    metadata:
      labels:
        app: daimoniac/suppline
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: daimoniac/suppline
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      
      containers:
      - name: daimoniac/suppline
        image: daimoniac/suppline:latest
        imagePullPolicy: IfNotPresent
        
        ports:
        - name: api
          containerPort: 8080
          protocol: TCP
        - name: metrics
          containerPort: 9090
          protocol: TCP
        - name: health
          containerPort: 8081
          protocol: TCP
        
        env:
        # Override scanner address to use separate Trivy service
        - name: SCANNER_SERVER_ADDR
          value: "trivy-server:4954"
        
        envFrom:
        - configMapRef:
            name: daimoniac/suppline-config
        - secretRef:
            name: daimoniac/suppline-secrets
        
        volumeMounts:
        - name: data
          mountPath: /data
        - name: config
          mountPath: /config
          readOnly: true
        - name: signing-key
          mountPath: /keys
          readOnly: true
        
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        
        livenessProbe:
          httpGet:
            path: /health
            port: health
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        
        readinessProbe:
          httpGet:
            path: /health
            port: health
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
      
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: daimoniac/suppline-data
      - name: config
        secret:
          secretName: daimoniac/suppline-secrets
          items:
          - key: daimoniac/suppline.yml
            path: daimoniac/suppline.yml
      - name: signing-key
        secret:
          secretName: daimoniac/suppline-signing-key
          optional: true
          items:
          - key: cosign.key
            path: cosign.key
