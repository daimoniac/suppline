apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: daimoniac/suppline-ui
  namespace: daimoniac/suppline
  labels:
    app: daimoniac/suppline-ui
    app.kubernetes.io/component: web-ui
  annotations:
    # Uncomment and configure based on your ingress controller
    # nginx.ingress.kubernetes.io/rewrite-target: /
    # cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  # Uncomment to enable TLS
  # tls:
  # - hosts:
  #   - daimoniac/suppline.example.com
  #   secretName: daimoniac/suppline-ui-tls
  rules:
  - host: daimoniac/suppline.example.com  # Replace with your domain
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: daimoniac/suppline-ui
            port:
              name: http
